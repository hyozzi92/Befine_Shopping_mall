import React from 'react';
import Carousel from '../Carousel/Carousel';
import './Main.scss';
const Main = data => {
  console.log(data);
  return (
    <div>
      <Carousel />
    </div>
  );
};

export default Main;
